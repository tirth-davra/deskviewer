appId: com.deskviewer.remote
productName: DeskViewer Remote Desktop
copyright: Copyright © 2024 DeskViewer
directories:
  output: dist
  buildResources: resources
files:
  - from: .
    filter:
      - package.json
      - app/**/*
      - main/**/*
      - renderer/**/*
publish: null

# Windows configuration
win:
  target:
    - target: nsis
      arch:
        - x64
  icon: resources/icon.ico
  artifactName: DeskViewer-Setup-${version}.${ext}
  compression: maximum

# macOS configuration  
mac:
  target:
    - target: dmg
      arch:
        - x64
  icon: resources/icon.icns
  artifactName: DeskViewer-${version}.${ext}

# Linux configuration
linux:
  target:
    - target: AppImage
      arch:
        - x64
  icon: resources/icon.png
  artifactName: DeskViewer-${version}.${ext}
